Chord Practice
<MidiSwitch
  @onDisable={{action "disableMidi"}}
  @onEnable={{action "enableMidi"}}
  @status={{this.isEnabled}}
/>

{{#if isEnabled}}
  <MidiInput
    @type="Midi Input"
    @list={{this.inputs}}/>

  <MidiInput
    @type="Midi Output"
    @list={{this.outputs}}/>

{{/if}}